class Hasher:
    def generate_hash(item):
        salt = generate_salt()
        salted = salt(item, salt)
        ##Perform hashing on now salted pass
    
    def generate_salt():
        salt=''
        ##generate salt for pass
        return salt
        
    def salt(item, salt):
        ##Salt the password
        salted=''
        return salted
